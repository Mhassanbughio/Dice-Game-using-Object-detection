# Dice-Game-using-Object-detection
A computer vision project that uses YOLO to detect and classify dice faces (1–6) from images. Includes custom dataset collection and labeling, model training, and a GUI application for automatic score calculation using live camera input or offline game image sequences.
